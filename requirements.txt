# ScreenWatch runtime dependencies (Python 3.8+), same on Windows and Linux.
# Tkinter is NOT here — it ships with Python (bundled by the python.org
# Windows installer; on Debian/Ubuntu it needs the separate system package
# 'python3-tk' — see README / install.sh / install.bat).

mss>=9.0.0        # fast, lightweight screen capture
numpy>=1.21       # cheap array diffing for change detection
pynput>=1.7.6     # mouse clicking + global hotkeys (native on Windows & X11)
Pillow>=9.0.0     # image helpers + the app icon (.ico export)
